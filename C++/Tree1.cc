# include <bits/stdc++.h>

using namespace std;

struct tnode{
    struct tnode* left;
    int data;
    struct tnode* right;
};

struct tnode* create_tree(){
    head = (struct root*)malloc(sizeof(struct root));
    head->val = 10;
    head->left = null;
    head->right = null;
    ltemp = head->left;
    
}
int main() {

}
