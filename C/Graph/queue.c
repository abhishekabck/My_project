# include <stdio.h>
# include <stdlib.h>
int front = -1;
int rear = -1;
int _size = 0;

void Queue(int size){
    int* array = (int*)malloc(sizeof(int)*size);
    _size = size;
    return array;
}

char* = strtof(123);